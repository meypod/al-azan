import {ScrollView} from 'native-base';
import {SafeArea} from '@/components/safe_area';

export function NotificationAdvancedSettings() {
  return (
    <SafeArea>
      <ScrollView p="4"></ScrollView>
    </SafeArea>
  );
}
